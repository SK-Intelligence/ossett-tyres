window.OSSETT_CONFIG = Object.freeze({
  // Set this to the deployed backend origin, without a trailing slash.
  // The backend currently allows only ossettyres.co.uk and www.ossettyres.co.uk.
  // An empty value intentionally keeps the form in explicit manual-contact mode.
  backendBase: "",
  contactEmail: "ossettwholesale@gmail.com",
  phone: "07380439443",
});
